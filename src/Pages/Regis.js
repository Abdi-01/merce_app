import React from 'react';
import { View } from 'react-native';
import { Text } from 'react-native-elements';

const RegisPage = (props) => {

    return (
        <View>
            <Text>Regis Page</Text>
        </View>
    )
}

export default RegisPage